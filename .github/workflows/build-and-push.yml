name: Build and Push Docker Image

on:
  workflow_dispatch: 
  push:
    branches: 
      - main
      - 'feature/*'
  schedule:
    # Läuft jeden Montag um 2:00 Uhr UTC
    - cron: '0 2 * * 1'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions: 
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets. GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/azpipeline:latest
            ghcr.io/${{ github.repository_owner }}/azpipeline:${{ github.sha }}
